before_script:
  - cd ./visual-monitor
  - npm install
  - cd ..

  - cd ./behat
  - composer install
  - cd ..

script:
  - cd ./visual-monitor
  - PROVIDER_PREFIX=browserstack SELECTED_CAPS=chrome mocha
  - cd ..

  - cd ./behat
  - ./bin/behat --tags=~wip
